<%= form_for(current_user.users_current_answer,
    :html => { :class =>  'answer-form' }) do |f| %>

  <%= f.text_area :body %>
  <%= f.hidden_field :question_id, :value => Question.todays_question.id %>

  <%= f.submit "Answer" %>

<% end %>