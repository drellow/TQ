<% if @current_user.answered_today? %>
<% answer = @current_user.users_current_answer %>
<% else %>
<% answer = Answer.new %>
<% end %>

<%= form_for(answer) do |f| %>

  <%= f.text_area :body %>

<%= f.submit "Answer" %>

<% end %>