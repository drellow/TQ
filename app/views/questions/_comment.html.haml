%span.comment-title#comment= comment.id
  = #{comment.user.username} said:

%span.comment-date= #{time_ago_in_words(comment.created_at)} ago
%p.comment-body
  =  auto_link(simple_format(comment.body), :tags => %w(br p) )
