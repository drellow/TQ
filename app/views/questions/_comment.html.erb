<span class="comment-title" id="comment<%= comment.id %>">
  <%= comment.user.username %> said:
</span>
<span class="comment-date"><%= comment.created_at.to_formatted_s(:long) %></span>
<p class="comment-body">
  <%=  sanitize(simple_format(comment.body), :tags => %w(br p) )%>
</p>